# Copy to .dev.vars for local `wrangler pages dev` (do not commit .dev.vars).
AIRTABLE_API_KEY=pat...

# Live agreement.html → /api/stripe/create-checkout-session
STRIPE_SECRET_KEY=sk_live_...

# agreement-testing.html → /api/stripe/*-testing (optional; falls back to STRIPE_SECRET_KEY if sk_test_)
STRIPE_TEST_SECRET_KEY=sk_test_...
